#pragma once

#include <constants/sound_list.h>

/// @file

/// @brief The identifiers for each newly added sound effect/music track.
/// @ingroup constants
/// @unofficial
enum NEW_SOUND_e {
    ORIGINAL_SOUND_COUNT = SOUND_COUNT-1,
    SE_OBJ_NOKOGIRI,
    NEW_SOUND_COUNT,
};
